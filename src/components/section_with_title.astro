---
interface Props {
  title: string;
  id: string;
  withPadding?: boolean;
}

const { title, id, withPadding = true } = Astro.props;
---

<section {id} class:list={[{ 'pt-4': withPadding }]}>
  <h4 class="text-lg text-primary font-bold">{title}</h4>
  <slot />
</section>
